#!/usr/bin/python3


def number_keys(a_dictionary):
    """Return the number of keys in a dictionary.
    """
    return dict_len(a_dictionary)


def dict_len(a_dictionary):
    """Return the number of values in a dictionary.
    """
    return len(a_dictionary)
